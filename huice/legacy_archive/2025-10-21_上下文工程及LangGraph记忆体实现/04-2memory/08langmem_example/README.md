# 测试记忆体功能演示

这个项目演示了如何在软件测试智能体中应用记忆体功能，通过记录、学习和应用历史测试经验来提高测试的智能化水平。

## 🎯 项目概述

测试记忆体功能是软件测试智能体的核心能力之一，它能够：

- 📚 **积累测试经验**：记录测试过程中的观察、思考、行动和结果
- 🗂️ **管理测试上下文**：保存项目配置、用户偏好、历史结果等信息
- 🚀 **优化执行策略**：基于历史执行记录优化测试策略
- 🚨 **学习错误处理**：积累错误处理经验，提供智能诊断和解决方案
- 🎯 **智能推荐**：基于历史数据推荐相关的测试用例
- 🧠 **自适应调整**：根据历史表现自动调整测试策略

## 📁 项目结构

```
testing_example/
├── __init__.py              # 包初始化文件
├── config.py                # 配置管理
├── models.py                # 数据模型定义
├── memory_manager.py        # 记忆体管理器
├── demo.py                  # 综合演示脚本
├── README.md               # 项目文档
├── scenarios/              # 场景演示目录
│   ├── __init__.py
│   ├── test_experience_scenario.py      # 测试经验场景
│   ├── test_context_scenario.py         # 测试上下文场景
│   ├── test_execution_scenario.py       # 测试执行场景
│   ├── error_handling_scenario.py       # 错误处理场景
│   ├── smart_recommendation_scenario.py # 智能推荐场景
│   └── adaptive_strategy_scenario.py    # 自适应策略场景
└── examples/               # 使用示例
    ├── basic_usage.py
    ├── advanced_usage.py
    └── integration_example.py
```

## 🚀 快速开始

### 1. 环境准备

确保已安装必要的依赖：

```bash
pip install langchain langgraph langmem pydantic
```

### 2. 运行演示

#### 交互式演示（推荐）
```bash
cd src/09-2memory/08langmem_example
python demo.py
```

#### 运行所有场景
```bash
python demo.py --all
```

#### 运行单个场景
```bash
python demo.py --scenario experience    # 测试经验场景
python demo.py --scenario context       # 测试上下文场景
python demo.py --scenario execution     # 测试执行场景
python demo.py --scenario error         # 错误处理场景
python demo.py --scenario recommendation # 智能推荐场景
python demo.py --scenario adaptive      # 自适应策略场景
```

### 3. 基础使用示例

```python
from testing_example import TestingMemoryManager, TestExperienceEpisode

# 初始化记忆体管理器
memory_manager = TestingMemoryManager()

# 记录测试经验
experience = TestExperienceEpisode(
    observation="执行用户登录功能测试",
    thoughts="需要考虑各种边界条件和异常情况",
    action="设计了包含10个测试用例的测试套件",
    result="发现了3个bug，测试覆盖率达到95%"
)

# 存储经验
memory_id = memory_manager.store_experience(experience)

# 搜索相关经验
related_experiences = memory_manager.search_experiences(
    query="用户登录测试",
    limit=5
)
```

## 📚 详细场景说明

### 1. 测试经验积累与学习场景

**功能描述**：基于Episode模式记录测试经验，包括观察、思考、行动和结果。

**应用价值**：
- 避免重复犯同样的错误
- 快速识别类似的测试场景
- 提供基于经验的测试建议
- 持续优化测试效率和质量

**核心模型**：`TestExperienceEpisode`

**演示内容**：
- 记录不同类型的测试经验（单元测试、集成测试、性能测试、UI测试）
- 基于历史经验进行智能测试
- 展示学习效果和经验应用

### 2. 测试上下文记忆场景

**功能描述**：保存和利用测试上下文信息，包括项目配置、用户偏好、历史测试结果等。

**应用价值**：
- 快速配置新项目的测试环境
- 个性化测试体验
- 基于历史数据做出更好的测试决策
- 提高测试配置的一致性和可重用性

**核心模型**：`TestContextMemory`

**演示内容**：
- 项目配置上下文管理
- 用户偏好设置保存
- 历史测试结果分析
- 智能上下文检索和应用

### 3. 测试用例执行记忆场景

**功能描述**：记录测试用例的执行过程，包括执行策略、发现的问题、优化建议等。

**应用价值**：
- 优化测试执行策略
- 识别性能瓶颈和问题模式
- 提供执行优化建议
- 建立测试执行基线

**核心模型**：`TestExecutionMemory`

**演示内容**：
- 不同类型测试的执行记录
- 基于历史执行优化策略
- 性能分析和优化
- 问题模式识别和预防

### 4. 错误处理经验记忆场景

**功能描述**：记录和学习错误处理经验，包括错误类型识别、解决方案记录、效果评估等。

**应用价值**：
- 快速诊断和解决类似错误
- 建立错误处理知识库
- 提供自动化错误恢复机制
- 预防常见错误的发生

**核心模型**：`ErrorHandlingMemory`

**演示内容**：
- 各种错误类型的处理经验记录
- 智能错误诊断和解决方案推荐
- 错误模式分析和预防策略
- 自动化错误恢复机制

### 5. 智能测试用例推荐场景

**功能描述**：基于历史记忆实现智能测试用例推荐功能，包括相似性分析、推荐算法、效果评估等。

**应用价值**：
- 提高测试用例的针对性和有效性
- 减少测试用例设计的重复工作
- 基于风险和历史数据优化测试覆盖
- 提供个性化的测试建议

**核心模型**：`TestRecommendation`

**演示内容**：
- 基于功能相似性的推荐
- 基于错误历史的风险测试推荐
- 基于性能数据的优化测试推荐
- 推荐效果评估和优化

### 6. 自适应测试策略场景

**功能描述**：根据历史表现自动调整测试策略，包括策略评估、动态调整、效果监控等。

**应用价值**：
- 持续优化测试策略
- 自动适应不同的测试场景
- 提高测试效率和质量
- 减少人工策略调整的工作量

**核心模型**：`TestStrategy`

**演示内容**：
- 测试策略基线建立
- 策略执行效果监控
- 基于表现的自动调整
- 策略优化和学习机制

## ⚙️ 配置说明

### 基础配置

```python
from 04testing_example.config import TestingMemoryConfig

config = TestingMemoryConfig(
    model_name="deepseek:deepseek-chat",           # 语言模型
    embedding_model="ollama:nomic-embed-text",     # 嵌入模型
    embedding_dims=768,                            # 嵌入维度
    max_memory_items=1000,                         # 最大记忆项数
    memory_search_limit=5,                         # 搜索结果限制
    confidence_threshold=0.8                       # 置信度阈值
)
```

### 高级配置

```python
# 自定义命名空间
config.memory_namespaces = {
    "test_experiences": ("my_project", "experiences"),
    "test_contexts": ("my_project", "contexts"),
    # ... 其他命名空间
}

# 测试配置
config.test_timeout = 300        # 测试超时时间
config.max_retry_attempts = 3    # 最大重试次数
```

## 🔧 API 参考

### TestingMemoryManager

主要的记忆体管理类，提供统一的记忆体操作接口。

#### 主要方法

```python
# 存储记忆
store_experience(experience: TestExperienceEpisode) -> str
store_execution(execution: TestExecutionMemory) -> str
store_error(error: ErrorHandlingMemory) -> str
store_context(context: TestContextMemory) -> str

# 搜索记忆
search_experiences(query: str, limit: int = None) -> List[Dict[str, Any]]
search_executions(query: str, limit: int = None) -> List[Dict[str, Any]]
search_errors(query: str, limit: int = None) -> List[Dict[str, Any]]
search_contexts(query: str, limit: int = None) -> List[Dict[str, Any]]

# 统计信息
get_memory_stats() -> Dict[str, Any]

# 数据管理
export_memories(memory_type: str = None) -> Dict[str, Any]
clear_memory(memory_type: str = None)
```

### 数据模型

#### TestExperienceEpisode
```python
class TestExperienceEpisode(BaseModel):
    observation: str      # 观察和背景
    thoughts: str         # 内部推理过程
    action: str          # 采取的行动
    result: str          # 结果与回顾
    test_type: TestType  # 测试类型
    test_result: TestResult  # 测试结果
    # ... 其他字段
```

#### TestExecutionMemory
```python
class TestExecutionMemory(BaseModel):
    test_case_id: str           # 测试用例ID
    test_name: str              # 测试用例名称
    execution_context: str      # 执行上下文
    test_strategy: str          # 测试策略
    discovered_issues: str      # 发现的问题
    optimization_insights: str  # 优化建议
    # ... 其他字段
```

## 🎯 最佳实践

### 1. 记忆体数据质量

- **详细记录**：确保记录的信息足够详细和准确
- **结构化存储**：使用标准化的数据模型
- **及时更新**：定期更新和维护记忆体数据
- **质量控制**：建立数据质量检查机制

### 2. 搜索和检索

- **关键词优化**：使用准确的关键词进行搜索
- **语义搜索**：利用嵌入模型的语义理解能力
- **结果过滤**：根据置信度和相关性过滤结果
- **上下文相关**：结合当前上下文进行搜索

### 3. 性能优化

- **批量操作**：尽量使用批量操作减少I/O
- **缓存机制**：对频繁访问的数据进行缓存
- **索引优化**：合理设置搜索索引
- **资源管理**：及时释放不需要的资源

### 4. 安全和隐私

- **数据脱敏**：对敏感数据进行脱敏处理
- **访问控制**：实施适当的访问控制机制
- **数据备份**：定期备份重要的记忆体数据
- **合规性**：确保符合相关的数据保护法规

## 🔗 扩展和集成

### 与现有测试框架集成

```python
# pytest 集成示例
import pytest
from testing_example import TestingMemoryManager

@pytest.fixture
def memory_manager():
    return TestingMemoryManager()

def test_with_memory(memory_manager):
    # 搜索相关经验
    experiences = memory_manager.search_experiences("登录测试")
    
    # 执行测试
    result = run_login_test()
    
    # 记录新经验
    if result.failed:
        memory_manager.store_error(create_error_memory(result))
```

### 自定义场景开发

```python
from testing_example.scenarios.base import BaseScenario

class CustomTestScenario(BaseScenario):
    def __init__(self, memory_manager):
        super().__init__(memory_manager)
        self.scenario_name = "自定义测试场景"
    
    def run_demo(self):
        # 实现自定义场景逻辑
        pass
```

## 📊 监控和分析

### 记忆体使用统计

```python
# 获取统计信息
stats = memory_manager.get_memory_stats()
print(f"总记忆数量: {sum(stats.values())}")

# 分析记忆体增长趋势
# 监控搜索性能
# 评估推荐效果
```

### 性能监控

- 记忆体存储性能
- 搜索响应时间
- 推荐准确率
- 系统资源使用

## 🤝 贡献指南

欢迎贡献代码、文档或提出改进建议！

### 开发环境设置

```bash
git clone <repository>
cd 08langmem_example
pip install -r requirements.txt
```

### 代码规范

- 遵循PEP 8代码风格
- 添加适当的类型注解
- 编写完整的文档字符串
- 添加单元测试

### 提交流程

1. Fork 项目
2. 创建功能分支
3. 提交更改
4. 创建 Pull Request

## 📄 许可证

本项目采用 MIT 许可证。详见 LICENSE 文件。

## 🙋‍♂️ 支持和反馈

如有问题或建议，请：

1. 查看文档和示例
2. 搜索已有的 Issues
3. 创建新的 Issue
4. 联系项目维护者

---

**Happy Testing with Memory! 🧠🚀**